# DuckDB Canvas

DuckDB Canvas is a tldraw-based canvas app for SQL exploration.

It combines:

- a SQL editor shape
- an always-visible query result panel
- DuckDB-WASM in the browser
- a DuckDB server reached through Quack

This repo currently contains the core prototype. Proper usage docs will come later.
